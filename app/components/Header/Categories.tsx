import styled from "styled-components";

const Container = styled.div`
	display: flex;
	margin: 0 auto;
	max-width: 1300px;
	width: 100%;
	align-items: center;
`;

export default function CategoriesNav() {
	return (
		<Container>
		</Container>
	);
}
